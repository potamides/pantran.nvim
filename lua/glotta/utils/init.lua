local utils = {
  buffer = require("glotta.utils.buffer"),
  table = require("glotta.utils.table"),
  properties = require("glotta.utils.properties"),
  api = require("glotta.utils.api"),
  protected = require("glotta.utils.protected")
}

return utils
